#!/bin/bash
pkg=${SRCDIR}/pkg/systemd
mkdir -p "$pkg"
cd "$pkg"
for url in https://archive.ubuntu.com/ubuntu/pool/main/s/systemd/systemd_258.1{-2ubuntu1{.dsc,.debian.tar.xz},.orig.tar.gz}; do
    test -f "$(basename "$url")" || wget -O "$(basename "$url")" "$url"
done
